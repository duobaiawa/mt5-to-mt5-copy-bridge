# 安全策略 · 零凭据跟单桥

## 报告漏洞

**请不要用公开 issue 报告安全问题。**
发邮件到仓库 `NOTICE` 里的维护者地址，附上复现步骤和影响范围。
我会在 72 小时内确认，修复后在发布说明里致谢（除非你希望匿名）。

## 这是什么风险等级的软件

这个程序**会在真实资金账户上下单**。一个被攻破的实例意味着攻击者可以：

- 用你的账户按他的指令开平仓
- 读取你的持仓、手数、账户标识

请按对待生产交易系统的标准来部署，不要按对待玩具项目的标准。

## 已实施的防护

| 措施 | 说明 |
|---|---|
| 令牌鉴权 | 管理接口与桥接接口分离，两套独立令牌 |
| Fail-closed | 未配置令牌时接口返回 503，**不会静默放行** |
| 常量时间比较 | `hmac.compare_digest`，无时序侧信道 |
| 参数化查询 | 全部数据库操作参数化，表名走白名单 |
| 输出转义 | 控制台所有动态内容经 HTML 转义 |
| 错误不外泄 | 客户端只拿到 error_id，堆栈只进服务端日志 |
| 幂等去重 | 成交按 ticket、命令按 command_id 去重，重放不会重复下单 |
| 交易双开关 | `trading_enabled` 默认关闭 |

## 已知限制

完整审计见 `SECURITY_AUDIT.md`。最重要的两条：

1. **密钥明文落盘** —— 本项目后端不存任何券商凭据（源端和执行端都由本地 EA 完成），
   但令牌本身存在 `.env` 里，请限制文件权限。
2. **应用层无 TLS、无限流** —— 必须靠反向代理提供，`deploy/nginx.conf.example` 已给出配置。

## 部署硬性要求

1. 绝不把服务端口直接暴露到公网。
2. `FOREX_MT5_DISABLE_AUTH` 只能在本机 127.0.0.1 上设为 1。
3. `.env` 权限 600，`data/` 权限 700。
4. 令牌用 `scripts/init_env.py` 生成，不要自己编。
5. 泄露后立刻 `python scripts/init_env.py --force` 轮换，并同步更新所有客户端。
